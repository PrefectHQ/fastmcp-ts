---
'@prefecthq/fastmcp-ts': minor
---

Allow embedded applications to customize the MCP client identity with `ClientOptions.clientInfo` and `MultiServerOptions.clientInfo`. The default identity now reports the installed FastMCP package version instead of a hard-coded version.
