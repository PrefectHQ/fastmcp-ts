---
title: "ModelSelector"
---

```ts
type ModelSelector = string | ((prefs) => string);
```

Defined in: [client/sampling/types.ts:10](https://github.com/PrefectHQ/fastmcp-ts/blob/9a190cd8187c0e4c6123cc65b41e3559ef08c78b/src/client/sampling/types.ts#L10)

Resolve a concrete model name from the server's modelPreferences.
A plain string always uses that model; a function inspects the preferences.
