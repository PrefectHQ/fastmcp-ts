---
title: "BrowserOAuth"
---

Defined in: [client/browser-oauth.ts:37](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L37)

## Extends

- [`OAuth`](/api/client/classes/OAuth)

## Constructors

### Constructor

```ts
new BrowserOAuth(options): BrowserOAuth;
```

Defined in: [client/browser-oauth.ts:44](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L44)

#### Parameters

##### options

[`BrowserOAuthOptions`](/api/client/interfaces/BrowserOAuthOptions)

#### Returns

`BrowserOAuth`

#### Overrides

[`OAuth`](/api/client/classes/OAuth).[`constructor`](/api/client/classes/OAuth#constructor)

## Properties

### \_callbackPromise

```ts
protected _callbackPromise: Promise<string> | null = null;
```

Defined in: [client/auth.ts:156](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L156)

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_callbackPromise`](/api/client/classes/OAuth#_callbackpromise)

***

### \_callbackReject

```ts
protected _callbackReject: ((err) => void) | null = null;
```

Defined in: [client/auth.ts:158](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L158)

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_callbackReject`](/api/client/classes/OAuth#_callbackreject)

***

### \_callbackResolve

```ts
protected _callbackResolve: ((code) => void) | null = null;
```

Defined in: [client/auth.ts:157](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L157)

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_callbackResolve`](/api/client/classes/OAuth#_callbackresolve)

## Accessors

### callbackServerPort

#### Get Signature

```ts
get callbackServerPort(): number | null;
```

Defined in: [client/auth.ts:328](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L328)

Returns the actual port the callback server bound to.
Only populated after redirectToAuthorization() has been called.

##### Returns

`number` \| `null`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`callbackServerPort`](/api/client/classes/OAuth#callbackserverport)

***

### clientMetadata

#### Get Signature

```ts
get clientMetadata(): {
};
```

Defined in: [client/browser-oauth.ts:55](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L55)

Metadata about this OAuth client.

##### Returns

```ts
{
}
```

#### Overrides

[`OAuth`](/api/client/classes/OAuth).[`clientMetadata`](/api/client/classes/OAuth#clientmetadata)

***

### redirectUrl

#### Get Signature

```ts
get redirectUrl(): string;
```

Defined in: [client/browser-oauth.ts:51](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L51)

The URL to redirect the user agent to after authorization.
Return undefined for non-interactive flows that don't require user interaction
(e.g., client_credentials, jwt-bearer).

##### Returns

`string`

#### Overrides

[`OAuth`](/api/client/classes/OAuth).[`redirectUrl`](/api/client/classes/OAuth#redirecturl)

## Methods

### \_armCallbackPromise()

```ts
protected _armCallbackPromise(): void;
```

Defined in: [client/auth.ts:242](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L242)

Creates the pending-callback promise that [waitForCallback](/api/client/classes/OAuth#waitforcallback) awaits.
Subclasses (e.g. BrowserOAuth) call this before opening their own redirect.

#### Returns

`void`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_armCallbackPromise`](/api/client/classes/OAuth#_armcallbackpromise)

***

### \_awaitCallback()

```ts
protected _awaitCallback(timeoutMs): Promise<string>;
```

Defined in: [client/auth.ts:265](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L265)

Races the pending callback promise against a timeout and clears the
promise state. Subclasses reuse this and add their own teardown.

#### Parameters

##### timeoutMs

`number`

#### Returns

`Promise`&lt;`string`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_awaitCallback`](/api/client/classes/OAuth#_awaitcallback)

***

### \_bind()

```ts
_bind(serverUrl): void;
```

Defined in: [client/auth.ts:178](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L178)

Binds the MCP server URL so that all storage keys are namespaced by it.
Called by Client before connecting.

#### Parameters

##### serverUrl

`string`

#### Returns

`void`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_bind`](/api/client/classes/OAuth#_bind)

***

### \_rejectCallback()

```ts
protected _rejectCallback(err): void;
```

Defined in: [client/auth.ts:257](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L257)

Rejects the pending callback with an error.

#### Parameters

##### err

`Error`

#### Returns

`void`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_rejectCallback`](/api/client/classes/OAuth#_rejectcallback)

***

### \_resolveCallback()

```ts
protected _resolveCallback(code): void;
```

Defined in: [client/auth.ts:252](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L252)

Resolves the pending callback with an authorization code.

#### Parameters

##### code

`string`

#### Returns

`void`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`_resolveCallback`](/api/client/classes/OAuth#_resolvecallback)

***

### clientInformation()

```ts
clientInformation(): Promise<OAuthClientInformationMixed | undefined>;
```

Defined in: [client/auth.ts:201](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L201)

Loads information about this OAuth client, as registered already with the
server, or returns `undefined` if the client is not registered with the
server.

#### Returns

`Promise`&lt;`OAuthClientInformationMixed` \| `undefined`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`clientInformation`](/api/client/classes/OAuth#clientinformation)

***

### codeVerifier()

```ts
codeVerifier(): string;
```

Defined in: [client/auth.ts:288](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L288)

Loads the PKCE code verifier for the current session, necessary to validate
the authorization result.

#### Returns

`string`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`codeVerifier`](/api/client/classes/OAuth#codeverifier)

***

### discoveryState()

```ts
discoveryState(): Promise<OAuthDiscoveryState | undefined>;
```

Defined in: [client/auth.ts:316](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L316)

Returns previously saved discovery state, or `undefined` if none is cached.

When available, `auth` restores the discovery state (authorization server
URL, resource metadata, etc.) instead of performing RFC 9728 discovery, reducing
latency on subsequent calls.

Providers should clear cached discovery state on repeated authentication failures
(via [`invalidateCredentials`](#invalidatecredentials) with scope `'discovery'` or `'all'`) to allow
re-discovery in case the authorization server has changed.

#### Returns

`Promise`&lt;`OAuthDiscoveryState` \| `undefined`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`discoveryState`](/api/client/classes/OAuth#discoverystate)

***

### invalidateCredentials()

```ts
invalidateCredentials(scope): Promise<void>;
```

Defined in: [client/auth.ts:295](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L295)

If implemented, provides a way for the client to invalidate (e.g. delete) the specified
credentials, in the case where the server has indicated that they are no longer valid.
This avoids requiring the user to intervene manually.

#### Parameters

##### scope

`"all"` \| `"client"` \| `"tokens"` \| `"verifier"` \| `"discovery"`

#### Returns

`Promise`&lt;`void`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`invalidateCredentials`](/api/client/classes/OAuth#invalidatecredentials)

***

### redirectToAuthorization()

```ts
redirectToAuthorization(authorizationUrl): Promise<void>;
```

Defined in: [client/browser-oauth.ts:59](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L59)

Invoked to redirect the user agent to the given URL to begin the authorization flow.

#### Parameters

##### authorizationUrl

`URL`

#### Returns

`Promise`&lt;`void`&gt;

#### Overrides

[`OAuth`](/api/client/classes/OAuth).[`redirectToAuthorization`](/api/client/classes/OAuth#redirecttoauthorization)

***

### resumeFromRedirect()

```ts
resumeFromRedirect(href?): string | null;
```

Defined in: [client/browser-oauth.ts:100](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L100)

Redirect-mode resume: parse the authorization code from a returned URL
(defaults to the current location). Returns the code, or null if absent.
Throws if the provider returned an error.

#### Parameters

##### href?

`string` = `window.location.href`

#### Returns

`string` \| `null`

***

### saveClientInformation()

```ts
saveClientInformation(info): Promise<void>;
```

Defined in: [client/auth.ts:213](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L213)

If implemented, this permits the OAuth client to dynamically register with
the server. Client information saved this way should later be read via
`clientInformation()`.

This method is not required to be implemented if client information is
statically known (e.g., pre-registered).

#### Parameters

##### info

`OAuthClientInformationMixed`

#### Returns

`Promise`&lt;`void`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`saveClientInformation`](/api/client/classes/OAuth#saveclientinformation)

***

### saveCodeVerifier()

```ts
saveCodeVerifier(codeVerifier): void;
```

Defined in: [client/auth.ts:284](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L284)

Saves a PKCE code verifier for the current session, before redirecting to
the authorization flow.

#### Parameters

##### codeVerifier

`string`

#### Returns

`void`

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`saveCodeVerifier`](/api/client/classes/OAuth#savecodeverifier)

***

### saveDiscoveryState()

```ts
saveDiscoveryState(state): Promise<void>;
```

Defined in: [client/auth.ts:312](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L312)

Saves the OAuth discovery state after RFC 9728 and authorization server metadata
discovery. Providers can persist this state to avoid redundant discovery requests
on subsequent `auth` calls.

This state can also be provided out-of-band (e.g., from a previous session or
external configuration) to bootstrap the OAuth flow without discovery.

Called by `auth` after successful discovery.

#### Parameters

##### state

`OAuthDiscoveryState`

#### Returns

`Promise`&lt;`void`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`saveDiscoveryState`](/api/client/classes/OAuth#savediscoverystate)

***

### saveTokens()

```ts
saveTokens(tokens): Promise<void>;
```

Defined in: [client/auth.ts:224](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L224)

Stores new OAuth tokens for the current session, after a successful
authorization.

#### Parameters

##### tokens

#### Returns

`Promise`&lt;`void`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`saveTokens`](/api/client/classes/OAuth#savetokens)

***

### tokens()

```ts
tokens(): Promise<
  | {
}
  | undefined>;
```

Defined in: [client/auth.ts:218](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/auth.ts#L218)

Loads any existing OAuth tokens for the current session, or returns
`undefined` if there are no saved tokens.

#### Returns

`Promise`&lt;
  \| \{
\}
  \| `undefined`&gt;

#### Inherited from

[`OAuth`](/api/client/classes/OAuth).[`tokens`](/api/client/classes/OAuth#tokens)

***

### waitForCallback()

```ts
waitForCallback(timeoutMs?): Promise<string>;
```

Defined in: [client/browser-oauth.ts:87](https://github.com/PrefectHQ/fastmcp-ts/blob/c9868d29d483dc78b21a1cfdf302208ac2dda0e5/src/client/browser-oauth.ts#L87)

Waits for the OAuth authorization code to arrive via the callback server,
then stops the server and resolves with the code.

Must be called after the UnauthorizedError thrown by connect() is caught.

#### Parameters

##### timeoutMs?

`number` = `...`

#### Returns

`Promise`&lt;`string`&gt;

#### Overrides

[`OAuth`](/api/client/classes/OAuth).[`waitForCallback`](/api/client/classes/OAuth#waitforcallback)
