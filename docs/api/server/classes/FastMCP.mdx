---
title: "FastMCP"
---

Defined in: [server/FastMCP.ts:225](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L225)

## Constructors

### Constructor

```ts
new FastMCP(options): FastMCP;
```

Defined in: [server/FastMCP.ts:254](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L254)

#### Parameters

##### options

[`FastMCPOptions`](/api/server/interfaces/FastMCPOptions)

#### Returns

`FastMCP`

## Properties

### name

```ts
readonly name: string;
```

Defined in: [server/FastMCP.ts:226](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L226)

***

### version

```ts
readonly version: string;
```

Defined in: [server/FastMCP.ts:227](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L227)

## Accessors

### address

#### Get Signature

```ts
get address(): ServerAddress | null;
```

Defined in: [server/FastMCP.ts:1359](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1359)

The bound address after run() resolves for the http transport. Null for stdio or before run().

##### Returns

[`ServerAddress`](/api/server/interfaces/ServerAddress) \| `null`

## Methods

### \_addCloseCallback()

```ts
_addCloseCallback(cb): void;
```

Defined in: [server/FastMCP.ts:1192](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1192)

Register a callback invoked when this server is closed — used by proxy connections.

#### Parameters

##### cb

() => `Promise`&lt;`void`&gt;

#### Returns

`void`

***

### \_dispatchPrompt()

```ts
_dispatchPrompt(
   name, 
   args, 
   ctx): Promise<{
  description?: string;
  messages: PromptMessage[];
}>;
```

Defined in: [server/FastMCP.ts:1076](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1076)

Dispatch a prompt render through this server's middleware chain using an inherited context.

#### Parameters

##### name

`string`

##### args

`Record`&lt;`string`, `string`&gt;

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;\{
  `description?`: `string`;
  `messages`: [`PromptMessage`](/api/server/interfaces/PromptMessage)[];
\}&gt;

***

### \_dispatchResource()

```ts
_dispatchResource(
   uri, 
   params, 
   ctx): Promise<unknown>;
```

Defined in: [server/FastMCP.ts:1038](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1038)

Run a resource read through this server's middleware chain using an inherited context.
Returns the raw handler result so the parent can apply convertResourceResult with the
actual requested URI (important for template resources).

#### Parameters

##### uri

`string`

##### params

`Record`&lt;`string`, `string`&gt; \| `undefined`

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;`unknown`&gt;

***

### \_dispatchTool()

```ts
_dispatchTool(
   name, 
   rawArgs, 
   ctx): Promise<{
[key: string]: unknown;
}>;
```

Defined in: [server/FastMCP.ts:986](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L986)

Dispatch a tool call through this server's middleware chain using an inherited context.
Used by parent servers to honour child-level middleware when routing mounted tool calls.

#### Parameters

##### name

`string`

##### rawArgs

`unknown`

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;\{
\[`key`: `string`\]: `unknown`;
\}&gt;

***

### \_removePrompt()

```ts
_removePrompt(name): boolean;
```

Defined in: [server/FastMCP.ts:969](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L969)

#### Parameters

##### name

`string`

#### Returns

`boolean`

***

### \_removeResource()

```ts
_removeResource(uri): boolean;
```

Defined in: [server/FastMCP.ts:955](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L955)

#### Parameters

##### uri

`string`

#### Returns

`boolean`

***

### \_removeTool()

```ts
_removeTool(name): boolean;
```

Defined in: [server/FastMCP.ts:948](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L948)

#### Parameters

##### name

`string`

#### Returns

`boolean`

***

### addProvider()

```ts
addProvider(provider): this;
```

Defined in: [server/FastMCP.ts:1201](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1201)

Register a provider (FastMCPApp or FastMCP) on this server.
All tools, resources, and prompts from the provider are mounted without a prefix.
For FastMCPApp, pass provider.server; for FastMCP, pass directly.

#### Parameters

##### provider

  \| `FastMCP`
  \| \{
  `server`: `FastMCP`;
\}

#### Returns

`this`

***

### close()

```ts
close(): Promise<void>;
```

Defined in: [server/FastMCP.ts:1575](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1575)

#### Returns

`Promise`&lt;`void`&gt;

***

### connect()

```ts
connect(transport): Promise<void>;
```

Defined in: [server/FastMCP.ts:1363](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1363)

#### Parameters

##### transport

`Transport`

#### Returns

`Promise`&lt;`void`&gt;

***

### getContext()

```ts
getContext(): McpContext;
```

Defined in: [server/FastMCP.ts:1352](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1352)

#### Returns

[`McpContext`](/api/server/interfaces/McpContext)

***

### mount()

```ts
mount(child, prefix?): this;
```

Defined in: [server/FastMCP.ts:1168](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1168)

Mount a child server onto this server. All tools, resources, and prompts from the child become accessible via this server. Pass a prefix to namespace names and prevent collisions.

#### Parameters

##### child

`FastMCP`

##### prefix?

`string`

#### Returns

`this`

***

### prompt()

```ts
prompt(config, handler): void;
```

Defined in: [server/FastMCP.ts:926](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L926)

#### Parameters

##### config

[`PromptConfig`](/api/server/interfaces/PromptConfig)

##### handler

(`args?`) => `unknown`

#### Returns

`void`

***

### resource()

```ts
resource(config, handler): void;
```

Defined in: [server/FastMCP.ts:937](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L937)

#### Parameters

##### config

[`ResourceConfig`](/api/server/interfaces/ResourceConfig)

##### handler

(`params?`) => `unknown`

#### Returns

`void`

***

### run()

```ts
run(options?): Promise<void>;
```

Defined in: [server/FastMCP.ts:1369](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1369)

#### Parameters

##### options?

[`RunOptions`](/api/server/interfaces/RunOptions)

#### Returns

`Promise`&lt;`void`&gt;

***

### tool()

#### Call Signature

```ts
tool<S>(config, handler): void;
```

Defined in: [server/FastMCP.ts:907](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L907)

##### Type Parameters

###### S

`S` *extends* `StandardSchemaV1`&lt;`unknown`, `unknown`&gt;

##### Parameters

###### config

`Omit`&lt;[`ToolConfig`](/api/server/interfaces/ToolConfig), `"input"`&gt; & \{
  `input`: `S`;
\}

###### handler

(`args`) => `unknown`

##### Returns

`void`

#### Call Signature

```ts
tool(config, handler): void;
```

Defined in: [server/FastMCP.ts:912](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L912)

##### Parameters

###### config

[`ToolConfig`](/api/server/interfaces/ToolConfig)

###### handler

(`args`) => `unknown`

##### Returns

`void`

***

### transform()

```ts
transform(t): this;
```

Defined in: [server/FastMCP.ts:977](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L977)

Add a transform to the pipeline. Applied to list responses in registration order.

#### Parameters

##### t

[`Transform`](/api/server/interfaces/Transform)

#### Returns

`this`

***

### use()

```ts
use(mw): this;
```

Defined in: [server/FastMCP.ts:1346](https://github.com/PrefectHQ/fastmcp-ts/blob/28d7c34e7077859e2478fbc7e137669d5d5bc2b2/src/server/FastMCP.ts#L1346)

Add a middleware to the pipeline.

Can be called at any point before the first request arrives. `setup()` is
called immediately on the primary server so that notification handlers and
other server-level side-effects are active before `connect()` / `run()` is
called. HTTP sessions created after `use()` will also have `setup()` called
via `_makeServer()`.

#### Parameters

##### mw

[`Middleware`](/api/server/interfaces/Middleware)

#### Returns

`this`
