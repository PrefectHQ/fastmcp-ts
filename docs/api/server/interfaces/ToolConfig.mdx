---
title: "ToolConfig"
---

Defined in: [server/FastMCP.ts:287](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L287)

## Properties

### annotations?

```ts
optional annotations?: ToolAnnotations;
```

Defined in: [server/FastMCP.ts:293](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L293)

Behavioral hints for clients (MCP 2025-03-26). Forwarded verbatim in tools/list.

***

### auth?

```ts
optional auth?: AuthCheck;
```

Defined in: [server/FastMCP.ts:315](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L315)

***

### description

```ts
description: string;
```

Defined in: [server/FastMCP.ts:291](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L291)

***

### disabled?

```ts
optional disabled?: boolean;
```

Defined in: [server/FastMCP.ts:312](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L312)

When true the tool is hidden from listTools and cannot be invoked via tools/call.

***

### input?

```ts
optional input?: StandardSchemaV1<unknown, unknown>;
```

Defined in: [server/FastMCP.ts:295](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L295)

Standard Schema validator for the tool's input arguments. Used for runtime validation.

***

### inputSchema?

```ts
optional inputSchema?: Record<string, unknown>;
```

Defined in: [server/FastMCP.ts:301](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L301)

Explicit JSON Schema advertised to clients as `inputSchema`. Overrides auto-generation from
`input`. Use when you need JSON Schema features beyond what your validator auto-generates
(e.g. `examples`, `$comment`, per-property descriptions, or custom annotations).

***

### name

```ts
name: string;
```

Defined in: [server/FastMCP.ts:288](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L288)

***

### output?

```ts
optional output?: StandardSchemaV1<unknown, unknown>;
```

Defined in: [server/FastMCP.ts:303](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L303)

Standard Schema validator for the tool's return value. Validated before result conversion.

***

### outputSchema?

```ts
optional outputSchema?: Record<string, unknown>;
```

Defined in: [server/FastMCP.ts:308](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L308)

Explicit JSON Schema advertised to clients as `outputSchema`. Overrides auto-generation from
`output`. Use when you need JSON Schema features beyond what your validator auto-generates.

***

### tags?

```ts
optional tags?: string[];
```

Defined in: [server/FastMCP.ts:314](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L314)

Arbitrary tags for server-side filtering.

***

### timeout?

```ts
optional timeout?: number;
```

Defined in: [server/FastMCP.ts:310](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L310)

Execution timeout in milliseconds. No timeout by default.

***

### title?

```ts
optional title?: string;
```

Defined in: [server/FastMCP.ts:290](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L290)

Human-readable display name shown in UIs. Takes precedence over `name` for display purposes.

***

### ui?

```ts
optional ui?: UiToolMeta;
```

Defined in: [server/FastMCP.ts:317](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L317)

Apps extension — links this tool to a UI resource and controls visibility.
