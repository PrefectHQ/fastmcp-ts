---
title: "RunOptions"
---

Defined in: [server/FastMCP.ts:173](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L173)

## Properties

### host?

```ts
optional host?: string;
```

Defined in: [server/FastMCP.ts:176](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L176)

***

### path?

```ts
optional path?: string;
```

Defined in: [server/FastMCP.ts:177](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L177)

***

### port?

```ts
optional port?: number;
```

Defined in: [server/FastMCP.ts:175](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L175)

***

### stateless?

```ts
optional stateless?: boolean;
```

Defined in: [server/FastMCP.ts:189](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L189)

Serve the legacy (2025-era) HTTP transport statelessly: a fresh server and
transport per request, no session registry, incoming `mcp-session-id`
ignored, and no session id issued. Use this behind a load balancer or on
serverless compute where consecutive requests reach different instances.

Falls back to the `FASTMCP_STATELESS_HTTP` environment variable, then to
`false`. A valid value is ignored for the stdio transport, but a malformed
one still aborts stdio startup, deliberately. The modern (2026-07-28) era
is already stateless and is unaffected.

***

### stdin?

```ts
optional stdin?: Readable;
```

Defined in: [server/FastMCP.ts:191](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L191)

Custom stdin stream for the stdio transport. Defaults to process.stdin.

***

### stdout?

```ts
optional stdout?: Writable;
```

Defined in: [server/FastMCP.ts:193](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L193)

Custom stdout stream for the stdio transport. Defaults to process.stdout.

***

### transport?

```ts
optional transport?: "stdio" | "http";
```

Defined in: [server/FastMCP.ts:174](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L174)
