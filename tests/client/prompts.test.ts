import { describe, it } from 'vitest'

describe('Client — Prompts', () => {
  describe('listPrompts()', () => {
    it.todo('returns an array of prompt definitions with name and description')
    it.todo('each definition includes the expected argument schema')
  })

  describe('getPrompt()', () => {
    it.todo('returns an array of messages with role and content')
    it.todo('passes provided arguments to the server prompt template')
    it.todo('throws when required arguments are missing')
  })
})
